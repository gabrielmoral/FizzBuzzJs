# FizzBuzzJs

### Before starting
 1. Install [Node.js](http://nodejs.org/)

 1. Install mocha

        npm install -g mocha

 1. Install dependencies

        npm install

### Passing the tests
  1. On the main directory run
  
        mocha
